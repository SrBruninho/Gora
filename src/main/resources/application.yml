server:
  port: 8090
spring:
  application:
    name: gora-project
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3309/goraDatabase?useTimezone=true&serverTimezone=UTC
    username: root
    password: gora123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect   
     